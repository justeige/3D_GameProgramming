#pragma once
// --------------------------------------------------
// all project common types/macros/functions
// --------------------------------------------------

#include <stdint.h>
#include <assert.h>
#include <vector>

using u8  = uint8_t;
using u16 = uint16_t;
using u32 = uint32_t;

using ID   = u32;

using Byte = u8;
using Bytes = std::vector<Byte>;